{erl_opts, [debug_info]}.

{deps, []}.

{project_plugins, [rebar3_format]}.

{format, [{formatter, default_formatter}, {options, #{paper => 100, break_indent => 2}}]}.

{extra_src_dirs, [{"test", [{recursive, true}]}]}.

{dialyzer, [{plt_extra_apps, [eunit]}, {plt_apps, all_deps}]}.
